import { Dimensions } from 'react-native';

export default {
    headerTitleSize: 16,
    headingFontSize: 24,
    headingSmallFontSize: 20,
    bodyFontSize: 16,
    buttonFontSize: 18,
    commentFontSize: 14,
    tinyFontSize: 12,

    screenWidth: Dimensions.get('window').width,
    screenHeight: Dimensions.get('window').height
};